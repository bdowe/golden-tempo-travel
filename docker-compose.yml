# Convenience wrapper — defaults to deployment stack.
# Prefer: make docker-dev | make docker-deploy
#
# See dockerize/README.md for details.

include:
  - path: dockerize/deployment/docker-compose.yml
